#ifndef SORT_H
#define SORT_H
#include "linked_list.h"

// Bubble sort algorithm for sorting an array of integers
template <typename T>
void bubbleSort(List<T>& list);

// Insertion sort algorithm for sorting an array of integers

template<typename T>
void insertionSort(List<T>& list);
#endif // SORT_H
